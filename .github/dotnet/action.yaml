name: dotnet setup
description: "base dotnet setup for project"
runs:
  using: composite
  steps:
    - name: setup
      id: setup-dotnet
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: "9.0.312"
